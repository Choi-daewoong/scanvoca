# conversation-clipper 전용 의존성 (완전히 독립된 로컬 환경)
# server/requirements.txt와 무관 — 이 도구는 Cloud Run에 배포되지 않는다.
# ffmpeg는 시스템 바이너리로 별도 설치 필요 (pip 패키지 아님).
pysrt==1.1.2
requests==2.32.3

# 테스트용
pytest==8.3.4
