# FastAPI Core
fastapi==0.121.1
uvicorn[standard]==0.38.0
python-multipart==0.0.20

# Database
sqlalchemy==2.0.44
psycopg2-binary==2.9.11
alembic==1.17.1

# Redis
redis==7.0.1

# Authentication & Security
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
google-auth==2.56.0

# Configuration
pydantic-settings==2.12.0
email-validator==2.3.0

# Google Gemini AI
google-generativeai==0.8.0
# Blog image generation (IMAGE output) — legacy SDK cannot request image modalities
google-genai==2.11.0
packaging==25.0

# HTTP client (GitHub Contents API — 블로그 발행)
httpx==0.28.1

# 수능/모의고사 기출 PDF 텍스트 추출 (ingest_exam_pdfs.py 1회성 스크립트 전용)
pdfplumber==0.11.4

# 블로그 대표 이미지 리사이즈 (gemini_service.generate_blog_image)
Pillow==12.3.0

# 방문자 User-Agent 파싱 (visit_service.get_stats — 기기/브라우저 통계)
# ua-parser/ua-parser-builtins도 명시 고정 — builtins는 정규식 DB라 배포마다 버전이
# 달라지면 동일 UA의 브라우저 판별 결과가 조용히 바뀔 수 있음 (테스트가 특정 문자열을 assert함)
user-agents==2.2.0
ua-parser==1.0.2
ua-parser-builtins==202606
